"use strict";
/**
 * Create the game object
 * */

let playerData = {
    "name": "Anthony",
    "health": 50,
    "items":  [
        "a rusty key"
    ]
}

let game = theCrypt.init(theCrypt.mapData, playerData);
